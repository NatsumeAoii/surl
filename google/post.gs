/**
 * Legacy split WRITE script.
 *
 * This project now deploys google/combined.gs as the only Google Apps Script
 * web-app entry point. Keep this file free of global doGet/doPost functions and
 * duplicate helper names so it cannot override the combined handler.
 */
var LEGACY_WRITE_SCRIPT_NOTICE = 'Deploy google/combined.gs instead.';
